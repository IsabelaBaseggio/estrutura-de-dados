    // FILA

// - Coleção cordenada de itens
// - Segue o princício FIFO (First In First Out), ou seja, o primeiro a entrar é o primeiro a sair

// se usa no formato de Classe, no caso de JS é em Objetos


// estrutura da Classe de fila
// Queue (fila)
function Queue() {

    var items = [];

    this.enqueue = function() {
        // adiciona um novo item
    }

    this.dequeue = function() {
        // remove um item
    }

    this.front = function() {
        // retorna o primeiro elemento da fila
    }

    this.isEmpty = function() {
        // verifica se a fila está vazio ou não
    }

    this.size = function() {
        // retorna o tamanho da fila
    }

    this.print = function() {
        // imprime a fila no console
    }
}