    // Hashes

// - transformar o valor em uma chave também
// - alternativa ao dicionários, com algumas diferenças
// - pega o elemento diretamente pela posição dele, sem iteração (no caso de dicionário), assim ganhando mai performace

//  // se usa no formato de Classe, no caso de JS é em Objetos

// Estrutura de Hashes

function HashTable(){

    var table = [];

    this.put = function(key, value) {

        // insere elemento com a chave passada

    }

    this.remove = function(key) {

        // remove elemento da chave passada

    }

    this.get = function(key){

        // retorna o valor da chave passada

    }

    var loseloseHashCode = function(key) {

        // retorna hash (um valor numérico)

    }

};